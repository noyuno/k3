#!/bin/bash -e

git pull origin master
git submodule init
git submodule update
git submodule foreach git pull origin master

if [ "$1" = "-i" ]; then
    # pull images
    c=docker-compose
    which $c 1>/dev/null 2>&1 || c=dc
    $c pull $*
fi
